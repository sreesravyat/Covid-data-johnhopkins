from setuptools import setup, find_packages

setup(
    name='<john-hopkin-covid>',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        'numpy',
        'pandas',
        'matplotlib'
    ]
)
